# Expanded Ideas

Ideas in this folder have enough context to continue through `/execute-goal`.
That command creates the task packet and implementation plan when the
preparation matrix requires them. Use `/plan-small-step` only when the user
explicitly wants preparation without implementation.
